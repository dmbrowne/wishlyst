import React from "react";
import { Heading } from "grommet";

const Account = () => {
  return (
    <div>
      <Heading level={2} children="My account" />
    </div>
  );
};

export default Account;
